version: 2.1

orbs:
  initialize:    julesabr/initialize@dev:4
  dotnet:        julesabr/dotnet@dev:2
  docfx:         julesabr/docfx@dev:1
  nuget:         julesabr/nuget@dev:2
  gh-pages:      julesabr/gh-pages@dev:1
  auto-version:  julesabr/auto-version@dev:1

workflows:
  main:
    when:
      equal: [ << pipeline.git.branch >>, main ]
    jobs:
      - initialize/checkout:
          repository-url: https://github.com/julesabr/Wrappers.git
          shallow-clone: no
      - auto-version/init:
          release-branch: main
          checkout: no
          attach-workspace: yes
          persist-to-workspace: yes
          requires:
            - initialize/checkout
      - dotnet/test:
          project-file: Julesabr.Wrappers.csproj
          checkout: no
          attach-workspace: yes
          requires:
            - initialize/checkout
      - dotnet/pack:
          lib-name: Julesabr.Wrappers
          version-file: version
          project-file: Julesabr.Wrappers.csproj
          checkout: no
          attach-workspace: yes
          persist-to-workspace: yes
          requires:
            - auto-version/init
            - dotnet/test
      - docfx/build:
          path: docs
          checkout: no
          attach-workspace: yes
          persist-to-workspace: yes
          requires:
            - auto-version/init
            - dotnet/test
      - approve-deploy:
          type: approval
          requires:
            - dotnet/pack
            - docfx/build
      - nuget/deploy:
          pkg-file: bin/dotnet/pack/*.nupkg
          api-key: $NUGET_KEY
          requires:
            - approve-deploy
      - gh-pages/deploy:
          path: docs/_site
          repository-url: https://github.com/julesabr/Wrappers.git
          requires:
            - approve-deploy
      - auto-version/publish:
          checkout: no
          attach-workspace: yes
          requires:
            - nuget/deploy
            - gh-pages/deploy
  develop:
    when:
      equal: [ << pipeline.git.branch >>, develop ]
    jobs:
      - initialize/checkout:
          repository-url: https://github.com/julesabr/Wrappers.git
          shallow-clone: no
      - auto-version/init:
          release-branch: main
          prerelease-branch: develop
          checkout: no
          attach-workspace: yes
          persist-to-workspace: yes
          requires:
            - initialize/checkout
      - dotnet/test:
          project-file: Julesabr.Wrappers.csproj
          checkout: no
          attach-workspace: yes
          requires:
            - initialize/checkout
      - dotnet/pack:
          lib-name: Julesabr.Wrappers
          version-file: version
          project-file: Julesabr.Wrappers.csproj
          checkout: no
          attach-workspace: yes
          persist-to-workspace: yes
          requires:
            - auto-version/init
            - dotnet/test
      - nuget/deploy:
          pkg-file: bin/dotnet/pack/*.nupkg
          api-key: $NUGET_KEY
          requires:
            - dotnet/pack
      - auto-version/publish:
          checkout: no
          attach-workspace: yes
          requires:
            - nuget/deploy
  feature:
    when:
      or:
        - matches: { pattern: "^feature.*$", value: "<< pipeline.git.branch >>" }
        - matches: { pattern: "^hotfix.*$", value: "<< pipeline.git.branch >>" }
    jobs:
      - initialize/checkout:
          repository-url: https://github.com/julesabr/Wrappers.git
      - dotnet/test:
          project-file: Julesabr.Wrappers.csproj
          checkout: no
          attach-workspace: yes
          requires:
            - initialize/checkout
      - dotnet/pack:
          lib-name: Julesabr.Wrappers
          project-file: Julesabr.Wrappers.csproj
          checkout: no
          attach-workspace: yes
          requires:
            - dotnet/test
